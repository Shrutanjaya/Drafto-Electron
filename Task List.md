# DraftoSLP Task List

> **Session update — 2026-06-03**
> _Legend: ✅ shipped earlier · 🟡 completed this session · ⬜ outstanding_
>
> - ✅ Tasks 1–8, 10, 12 — shipped in earlier sessions (v1.1.7 and prior)
> - 🟡 Task 9 (Numbered Lists) — completed this session
> - 🟡 Task 11 (Find & Replace) — completed this session
> - 🟡 Task 13 (AoR Signature & True Copy text) — completed this session (Beta)
> - 🟡 Task 14 (AI Plugin) — implemented this session as a Beta (CLI / bring-your-own-Claude); see the detailed notes at the bottom. Pending live verification.
>
> Also this session: repositioned the Split/Nav toggle to sit next to the formatting toolbar (pinned far right) and made the formatting toolbar always visible (greyed until a text field is focused).
>
> **Released as v1.1.9** (commit `f4a2bfe`, tag `v1.1.9`, CI builds Win + Mac installers; GitHub release is created as a draft and must be published manually). v1.1.8 had already shipped before this batch, so this work went out as v1.1.9.
>
> **Additional changes this session, beyond the numbered tasks:**
> - **Output Text Formatting** (new Settings control): user-selectable font (14 faces), font size, line spacing and after-paragraph spacing. Defaults TNR 14pt / 1.5 / 12pt. Drives the body `Normal` style; rich-text content inherits it. Tooltip recommends the defaults, notes that Cover Page / Listing Proforma / Index / Office Report only follow the font *type* (size & spacing fixed to preserve one-page structure), and warns the font must be installed locally.
> - **One-page protection**: Cover Page and Office Report use a constrained style (Arial → 13pt, else 14pt; fixed 1.5 line / 12pt after). Listing Proforma font now follows the user's choice (size/spacing still fixed).
> - **Blockquote trailing space** is now derived from the user's font size + line spacing + after-spacing (replaces the hardcoded 18pt referenced in the Task 9 note), so the gap after a quote matches a normal paragraph gap.
> - **Settings reorganised** into Appearance · Workspace · Document Formatting · Paperbook · Save Locations · Shortcuts · Support (replacing View/Customize/Durations); advanced Volume Splitting fields hidden behind "Show advanced"; subtext added throughout; usage stats moved to Support.
> - **New Shortcuts tab** in Settings listing every keyboard shortcut.
> - **True Copy** extended: bottom-left or bottom-centre position, optional white background (mirrors annexure-label background); rotation-aware. **AoR signature** in Filed-by blocks made a floating, behind-text image (no layout displacement) with a small overlap onto the name. Pro-Tip reworded and a prominent disclaimer added (responsibility for paperbook contents rests with the user).
> - **Polish/fixes**: "S.No." → "S. No." in Memo of Parties and Index/Master Index; Listing Proforma S.No. column widened; parties-header "(s)" no longer breaks onto a new line (word joiners + wider column); blank line added after "BRANCH OFFICER"; removed the unused "Open projects folder" button from Load Projects.
Interim Relief Checkbox: The Petition tab has an Interim Relief checkbox. Please ensure that its state is unchecked (False) by default.
Blank Lines: After the prayers, the sentence "And for this act of kindness..." appears. After this sentence, it seems that Drafto is adding two lines/paragraphs instead of just one. Please change it to one.
Orphan Control in Annexure Sentences: If a user adds multiple annexures in the same row in the LoDTable, the resultant sentence in the docx file (Annexure P-X is a true copy of...) has some weird orphan setting applied such that the last line is stretched out till the end (this is for all annexures except the last one in that row). Fix this and make it appear normally. Make sure the other tables like in the IAs, which also allow annexures, don't have this problem either.
Ctrl+R: When inside a BadhiyaBox, the user might want to use the Ctrl+R shortcut to enforce right-alignment on the text. But currently, Ctrl+R seems to be taken by Electron's Refresh command. Please disable that so that Ctrl+R seamlessly works for right alignment.
Dark Mode Highlight: This requires a colour scheme fix. Currently, when using Drafto in dark mode, the highlight feature causes the text to become unreadable, perhaps because while the highlight colour is applied, the text colour remains white. So, yellow highlights over white text doesn't look nice. This will require a colour scheme adjustment in the dark mode such that, regardless of the highlight colour selected, the highlight effect remains visually appealing.
Toggle Switch: The Petition tab supports two views i.e. Splitter and Navigation view. Currently, the user must go to the Settings box and change the view from there, which is cumbersome. Let that Setting instead be to select the Default view i.e. when the user loads a blank project. However, for real time use, provide a Toggle Switch before (to the left of) the Undo-Redo buttons in the menu bar. The user should seamlessly be able to toggle between the Splitter and the Navigation views without having to click Settings.
Default Save Location of .drafto files: Currently, in Settings, the user can choose the default save locations for docx and pdf files, but not for .drafto project files. Please remove this inconsistency and allow the user to select the default save location of .drafto files from Settings itself. This should not affect the working of the Change Save Location button in the Project menu.
Volume Splitting: There is a rule in Supreme Court filings that if the total page count of the SLP (i.e. the written page numbers as per the Index) crosses 400 pages, the paperbook should be split into two volumes, each having 200 or more pages. Similarly, if it crosses 600 pages, split into 3 Volumes, 800 causes 4 Volumes, and so on. The Index currently being generated in the SLP will continue to be generated in the same format. Volume I will have an index for the entire SLP (all volumes), titled Master Index. Then it will have an Index for Volume 1 only, in the same tabular format. Similarly, each Volume will have an index for that volume only (but not the master index, which will only be in Volume 1). Drafto should conduct this check during PDF generation - if it realises that the total page count has exceeded 400 (say), it should trigger the function to split up the SLP into volumes with different indexes as aforesaid. The cover page of Volume I of the SLP will remain unchanged. The other volumes will have a similar cover page too - just that they won't have any IA details (that IA table will be missing). In the paragraph preceding the text "PAPERBOOK", it will have the text "Volume III" in bold caps (basically the volume number in Roman Numerals). Please explain the task back to me so that I am sure that you have understood it. If you haven't, ask me doubts.
🟡 **[Done this session]** Numbered Lists: If the user creates a numbered list in a particulars cell in the List of Dates table (1, 2, 3) which each having sub-points (a, b, c...), each of which have sub-clauses (i, ii, iii). And inside 2(a)(iii), say, the user wants to add a quotation mark. This is a complex input. Please tell me how this text will appear in the output docx/pdf files, particularly with respect to the indent parameters. Please also tell me how it will appear on the Drafto app interface.
> **This session:** Explained the rendering (app + docx). Then implemented: fixed the hanging-indent bug on wrapped list lines; quote (blockquote) export now wraps the whole block in smart quotes, italicises it, and respects a Settings → Document Formatting line-spacing option (default vs. forced single, with the after-block spacing later changed from a hardcoded 18pt to a value derived from the user's font size and spacing — see the v1.1.9 notes above); quote marks aren't doubled if the user already typed them at a boundary (single-quote-aware), with an in-editor corner cue; a defined 5-level numbering scheme (1 → a → i → A → I) capped at 5 levels in both app and docx; blockquotes work inside numbered/bulleted lists with no marker and no consumed number, indented additively (sub-level + 0.25″ left, 0.5″ right); pressing Enter to exit a quote starts a new list item; Backspace on a blank line after a list rejoins the previous point; bullet-list parity throughout.
Customization: The Settings dialog should have another tab called Customize. The setting to add a white background behind annexure labels should be moved to the Customize tab. This tab should also allow the user to customize the text size of Annexure Labels - some users have given a feedback that they prefer larger sizes than what I have currently hardcoded. So let the user decide, while the default size can be what the current setting is. But remember that the current setting accounts for the page height, text size etc. while determining the positioning of the Annexure Label on the pdf page. So the logic should be flexible enough to account for a custom text size set by the user. If you want, you can set the maximum text size as 24, so that the user can't go beyond that. The Customization tab should also allow the user to define the Volume-Splitting limits i.e. instead of 400 pages as the first cut and 200 pages as the subsequent cuts.
🟡 **[Done this session]** Find and Replace: Drafto is meant to eliminate the need to use MS Word. However, the user might feel that the Find and Replace feature which exists in MS Word is missing in Drafto. I need you to provide the feature. The user should be able to specify whether he wants the find and/or replace in all the tabs and contents or only selective tabs or tables etc. Ctrl+F should work as a shortcut.
> **This session:** Implemented. Ctrl+F opens a Find & Replace panel that searches the form data across every tab (visible or not). It offers a scope selector (all sections, or specific tabs/tables), a live match count with 1-of-N Next/Prev navigation, on-screen highlighting of the current match (text selection inside rich-text editors, native selection in plain inputs), a Match-case toggle, and Replace / Replace All. A tooltip explains two known limitations (a word split across formatting, and the raw symbols & < >). Navigating to a Petition match switches that tab to Navigation view on the relevant section so the field can be revealed.
Custody Certificate etc.: Some Criminal SLPs have a special requirement. If the Petitioner is in jail, he must attach with his SLP a Custody Certificate issued by the jailor. He must also attach an information sheet titled FIR Details. So, if the user is making a Criminal SLP, the PDF Generation dialog should show two entries i.e. Custody Certificate and FIR Details at the end just before Vakalatnama. However, filling these entries should be optional. If the user clicks "Go" without attaching these files, Drafto should show a warning that these two documents are missing. The user may choose to proceed anyway. If the user adds these files, they should be paginated, bookmarked, and added to the Index just like all the other components.
🟡 **[Done this session — Beta]** AoR Signature and True Copy text: These two components need to be added to each SLP. The first is the AoR Signature, which needs to be added wherever the "Filed By" table appears, just above the name of the AoR in that table. That means it should go at the bottom of the Office Report on Limitation, after the SLP Prayers, at the bottom of the Certificate, after the IA Prayers, and in the Filing Memo. The second is the True Copy text, which gets placed (along with the AoR Signature) at the bottom left of each page in each annexure. It just says "True Copy", above which appears a very small AoR Signature. To enable these two elements to be placed at the respective places, the AoR should upload their signatures in .png format in the Customization Tab. The Customizaton tab should allow the user to decide whether the signature photo gets placed in the Paperbook, and also whether the True Copy text gets placed. A tooltip should advise (Pro-Tip) that the signature should have a transparent background for the cleanest appearance. The user should also be able to determine the size of the signature photo that gets placed in the Paperbook (preferably with visual feedback).
> **This session:** Implemented as a Beta feature. New "AoR Signature & True Copy (Beta)" section in Settings → Customize: upload a PNG (stored in `drafto-settings`), live size preview, two toggles (place signature in paperbook / stamp True Copy on annexures), a single width slider with visual feedback, and a transparent-background Pro-Tip. The signature is embedded as an `ImageRun` just above the AoR name in `createFiledByTable` (so it appears in all six "Filed by" blocks: Office Report, SLP Prayers, Certificate, IA Prayers, Filing Memo, Listing Proforma). On PDF generation a rotation-aware `addTrueCopyStamp` helper draws a small signature (½ the configured width) above the words "True Copy" at the bottom-left of every annexure page (P-annexures incl. typed/translated, IA A-annexures, and the certified-copy receipt). Both placements are gated on the toggles + a valid PNG.
**Layer 5 — annexure split-and-attach (Phase 1), DECISIONS LOCKED (2026-06-04):**
> Goal: user dumps garbled/consolidated PDFs (annexures, executed affidavits/vakalatnama, custody cert, FIR, etc.) into the folder; the assistant splits them into isolated documents and attaches each at the right place. Architecture = **"Claude maps, Drafto cuts & attaches"** (Claude stays read-only; Drafto does deterministic pdf-lib splitting + attaching; user approves a reviewed map with thumbnails).
> - **Chronological hard rule:** an annexure with date X attaches to the LoD row whose Date = X. Drafto already auto-numbers P-1, P-2… by chronological position, so the model must NOT assign P-numbers (like grounds). Annexure target = `lodTableItem.annexures` (filePath/title/date/copyType/isAdditionalDocument; typed/translated via typedOrTranslatedFilePath). IA annexures → `iaGroundItem.annexures`.
> - **User decisions:** (1) split files saved to a **managed subfolder next to the .drafto project**; (2) if no LoD row matches an annexure's date, **auto-create the row chronologically with a meaningful narrative-fitting description — do not pause to ask**; (3) **render first-page thumbnails** of each proposed split in the review (users won't trust a text-only map); (4) **Phase 1 = annexures first**, then Phase 2 = executed affidavit (`slpAffidavit` merge slot `userFilePath`) + vakalatnama, then Phase 3 = custody certificate + FIR (BLOCKED on first building the separate "Custody Certificate" task — those slots don't exist yet).
> - **Flow:** Claude returns a Document Map (per doc: sourceFile, startPage, endPage, type, title, date, isAdditionalDocument, copyType) → Drafto validates (page-bounds vs scan counts, overlaps, date normalisation) → review card w/ thumbnails → on approve, `ai-split-documents` IPC uses pdf-lib copyPages into the managed folder → attach to LoD rows by normalised date (auto-create if missing). A "document" = file + page range, so single-doc-per-file dumps need no split.
> - **Risks:** LLM page-boundary accuracy (mitigated by mandatory review + thumbnails); scanned docs need gated image reading.
>
> **Phase 1 PROGRESS — backbone built & unit-tested (UI remains):**
> - `src/lib/ai/document-map.ts`: types (DocType, CopyType, SafeDocument), `normalizeDate()` (dd.mm.yyyy / yyyy-mm-dd / "12 March 2021" / "March 12, 2021" → ISO; verified), `validateDocumentMap()` (page-bounds vs scan counts, unknown-file + overlap rejection; verified).
> - `electron/ipc/pdf-split.js#splitDocuments`: pdf-lib `copyPages` cuts each range into its own PDF in `<project>-annexures` next to the .drafto file (temp fallback if unsaved); source untouched; loads each source once. Verified: 10-page source → 3 docs with correct page counts in the managed folder. IPC `ai-split-documents` wired (+ preload + types).
> - `src/lib/ai/apply-documents.ts#applyDocuments`: attaches annexures to the LoD row matching the normalised date; auto-creates the row at the correct chronological position (description from title) when missing; sets AD flag; defers non-annexure types (affidavit/etc.) for Phase 2/3. Verified: matched existing row, created+placed a missing one between two dated rows, deferred an affidavit.
> - Contract + rules: `drafto-knowledge.ts` gained the optional `"documents"` map output format; `master-instructions.ts` gained the splitting/classification/chronological/no-P-number/AD rules; `form-patch.ts` passes `documents` through.
> - Build + `node --check` clean.
> **Phase 1 UI — DONE (end-to-end, pending live test):**
> - `src/lib/ai/pdf-thumb.ts`: renders a source PDF page → PNG data URL via pdf.js in the renderer (worker bundled by Vite through `?url` import; loads each source once). Reads source bytes via `readFileByPath` (raw base64).
> - Project path plumbed: `header.tsx` writes the current `.drafto` path to `window.__draftoProjectPath` so the panel can place split files next to it (temp-folder fallback when unsaved, surfaced in the result message).
> - Chat panel: when a reply includes a `documents` map, it's validated against `folderScan.files` page counts, thumbnails render asynchronously, and a **"Documents to split & attach" review card** shows each doc (thumbnail · title · type · AD · date · source pp.) with Split & attach / Dismiss. On approve → resolve sourcePaths + project path → `aiSplitDocuments` → `applyDocuments` → summary message (attached count, rows created, deferred non-annexure types, where files were saved). Doc-map cleared on new turn / folder change.
> - Vite build (worker bundled) + `node --check` all clean.
> **FIX — model refused to map annexures ("can't attach, files aren't separate"):** The model misread the contract — thought *it* had to produce separate files (impossible, read-only) so it punted. Two fixes: (1) Master Instructions now state CRITICALLY that the model never splits/creates files, only identifies each document by source file + page range, and **Drafto** does the splitting — a consolidated PDF is the normal case; it must NEVER say annexures can't be attached. (2) The prompt's source note now **explicitly lists the original PDF names + page counts** (they were never listed before, so the model didn't know what to map). Verified both in the built prompt.
>
> **FIX — split annexures attached the description but not the file (empty clip icon):** Drafto's clip-icon/attachment state is driven by the annexure's `.file` (a File object), which the upload dialog sets alongside `.filePath`. `applyDocuments` only set `.filePath`. Fix: panel now reads each split PDF's bytes (`readFileByPath`) and builds a `File` (with `.path` set to mimic an Electron File), passed via `AttachableDoc.file`; `applyDocuments` sets `annex.file` too. This makes the clip register AND persists (save logic extracts `filePath` from `file.path`). NOT a Claude folder-access issue — Drafto splits/attaches; Claude never touches the split files. Build clean.
>
> **FIX — big Sonnet task killed by false idle timeout (~5 min):** The idle timer resets on stdout writes, but without partial streaming the CLI emits nothing during a long *final generation* (it only writes when a message completes) — so a big draft (all fields + annexure map) went silent >4 min mid-generation and was wrongly stopped. Fix: added `--include-partial-messages` so the CLI streams token-level deltas continuously (verified: a generation emits a steady stream of `stream_event` lines; final `result` still parses; my handler ignores partial events but `bump()` fires on every stdout write). Idle bumped 240s→300s for headroom. Now active work is never silent, so the idle timeout only catches genuine hangs.
>
> **FIX — runaway task with no upper bound (Sonnet 30+ min):** Fixing the false idle-timeout via partial streaming removed the only ceiling, so a slow/looping/over-large task could run unbounded. Added a **hard 20-min total cap** (`MAX_TOTAL_MS`) alongside the idle timer, with a message advising to split the task. The Stop button remains the user's manual control. Root advice: the combined "read 182k tokens + reproduce identical language + draft every field + map all annexures" is too much for one turn — split into (a) fill the tabs, then (b) map/attach annexures. `node --check` clean.
>
> **Live activity log (granular progress):** Replaced the single "Reading documents…" line with a running step list (done ✓ / current ⟳). Main process parses the token stream: tool calls → "Reading <file>…"; the streamed JSON is scanned for `"path":"…"` → "Filling Synopsis / List of Dates / Grounds / Questions of Law / …" (via `aiFieldLabel`); `"documents":` → "Mapping annexures…". Statuses are deduped (each distinct one = a step). Panel renders the log in the thinking area with a Stop button; previous steps show a green check, current shows a spinner. Build + `node --check` clean.
>
> **Salvage-on-Stop (recover completed work):** Previously Stop/timeout discarded everything (fields apply only after the full response + an Apply click). Now: `ai-run` returns `partialText` (the accumulated stream) on cancel AND on idle/hard-cap timeout. New `extractPartialProposal()` in `form-patch.ts` uses a brace-matching, string/escape-aware scanner (`extractCompleteArrayObjects`) to recover only the **fully-completed** `operations` and `documents` objects from cut-off JSON, dropping the half-written one (verified: caseType/synopsis/petitioners recovered, incomplete grounds dropped; braces+quotes inside values handled). Panel refactored: extracted `presentFieldOps()` + `presentDocMap()` reused by both the success path and the salvage path; on Stop/timeout it recovers completed items into the normal review cards (field suggestions + doc-map thumbnails) and the user Applies what they want; says so if nothing was recoverable. Apply click still the safety gate. Build + `node --check` clean.
>
> **Token counter + confirmation-memory + continuity diagnosis:**
> - **Continuity was fine all along.** The cost-confirmation is folder-based and fired on every substantial request (the "ask every time" choice) — it does NOT indicate a fresh session; resume continuity was working. Fixed the annoyance: now asked **once per conversation** (`costAcknowledged` + remembered `imageModePref`), reset on folder change.
> - **The "stuck at Reading your request… 2 min" = large-context resume latency.** Resuming a conversation that read a 182k-token writ petition carries that whole context forward (shows up as `cache_read_input_tokens`), so the model spends minutes *processing input* before any tool call/output → log looked frozen. Now status flips to "Thinking…" on `message_start`, and the token counter shows the big input immediately.
> - **Live token counter:** main process parses `message_start` (input + cache_read + cache_creation = context size) and streamed text (output estimate ≈ chars/4), throttled `usage` events to the panel; `result.usage` gives the accurate final. Panel shows "↑ X in · ↓ Y out" live in the activity box and a "Last turn" line in the footer. Verified against a real stream (message_start usage + result usage parse correctly).
> - **"% credits used" is NOT available** — the CLI exposes token counts and an API-equivalent `total_cost_usd`, but no subscription credit/percentage. Per-task token breakdown also isn't feasible (one generation); the running counter + activity log give per-phase insight instead.
> - Build + `node --check` clean.
>
> **Preset quick-actions:** New `src/lib/ai/presets.ts` — 10 one-click actions, each a carefully-worded instruction (Master Instructions still enforce style/format), grouped Fill / Draft / Annexures: Memo of Parties, Impugned Order, Deponent, Listing Proforma; Synopsis, List of Dates, Grounds, Questions of Law, Interim Relief; Split & mark Annexures. Panel: a wand toggle in the composer reveals grouped preset chips; clicking one fires the curated prompt through the normal pipeline (cost-confirm, session resume, etc.). Refactored `handleSend`→`submitText(raw)` so presets and the input box share the path. Added `interimReliefGrounds`/`interimReliefPrayers` to the field catalog (+ apply-ops parsers) so the Interim Relief preset actually fills. Build + prompt checks clean.
> - **Not-yet-covered presets (need more work first):** "Draft the Applications" (IAs — needs customIas/standardIas in the catalog) and "Split executed Affidavits/Vakalatnama" (Phase 2/3 attachment slots) are intentionally omitted until those land.
>
> **Elapsed timer + reasoning indicator + pre-task time estimate:**
> - **Elapsed clock** (mm:ss) ticks every second while a turn runs, shown next to the token counter in the activity box — so a flat output count no longer reads as "frozen," you can see time passing.
> - **"Reasoning…" status:** main process now catches `thinking_delta` events and surfaces "Reasoning…", so a pre-output reasoning phase shows as active work rather than silence.
> - **Rough time estimate** (`src/lib/ai/estimate.ts`): `estimateSeconds/Label` from context size × task effort × model speed (very approximate, labelled "rough"). Shown in THREE places, all *before/at* task start: on each **preset chip** (e.g. "Draft the Synopsis · ~2 min"), in the **cost-confirmation card** ("Estimated time: ~X"), and live during the turn ("0:42 · est ~2 min"). Presets carry an `effort` tag (small/medium/large); typed prompts default to medium. Estimate uses folder text size, or the prior turn's actual input tokens once a conversation is underway (so resumed-turn estimates account for the carried context). Verified: 182k+large+Sonnet→~2min, +Opus→~4min, +Haiku→~1min, tiny→~30s.
> - Build + `node --check` clean.
>
> **Named "Mayur" + made conversational:**
> - Chatbot renamed **Mayur** across the UI: launcher pill, panel header, empty-state greeting ("Hi, I'm Mayur"), composer footer, Settings nav ("Mayur (AI)") + section header + enable toggle + toasts, and all "Settings → AI Plugin" / "the assistant" references. (Internal code/identifiers like `aiPluginEnabled` unchanged.)
> - System prompt reworked for a **conversational persona**: opens "You are **Mayur**…"; new "How to be" section — talk like a sharp friendly junior counsel, discuss/explain/advise, **ask clarifying questions when a request is ambiguous or missing facts** instead of guessing, use judgement like a normal Claude chat, refer to self as Mayur. "How to respond" reframed so **most replies are plain conversation (no JSON)**; the JSON proposal is only for when actually filling fields. The existing flow already supports this (Mayur's prose shows as assistant messages; session-resume keeps context), so back-and-forth/clarifying works mechanically. Verified all bits in the built prompt. Build clean.
>
> **Talk-vs-Act fix + safety net + bigger panel:** Mayur was dumping drafts in chat instead of filling fields. Fixes:
> - **System prompt — "Two modes: TALK and ACT":** TALK = discuss/advise/ask (no JSON); ACT = put content into Drafto (JSON proposal). ACT triggered by action verbs (draft/fill/write/enter/populate/add/mark/attach/set/update/generate/put in) AND "yes/go ahead/do it" affirming an offer to fill; **default to ACT** on any such verb, ask only if truly ambiguous. Critical ACT rules: emit ONLY a one-line confirmation + the JSON (never the full draft as prose); **never write the same content twice**; and **do NOT re-read the source documents to fill content already produced/discussed in the conversation** (the user's explicit ask). Sole exception: explicit "show me in chat". Rebalanced the old over-conversational framing.
> - **Safety-net button** ("Put this into Drafto"): if the user's message looked action-y (verb/affirmation regex) but Mayur returned prose with no proposal, a button appears that re-asks Mayur to convert its last reply into a field proposal **reusing what it wrote, no folder re-read** (`FILL_FROM_CHAT_PROMPT`). User-triggered (cheap). No explicit Talk/Fill toggle (per decision).
> - **Bigger panel:** maximize toggle in the header (Maximize2/Minimize2) switches 380×520 ↔ 680×820; default bumped up slightly; annexure thumbnails render larger (w-24) when expanded for clearer review.
> - Build + prompt checks clean.
>
> **JSON-dump bug fix + prompt-structure guidance:**
> - **Bug:** Act mode produced JSON but it appeared as raw text and nothing filled → Drafto failed to PARSE it (almost certainly invalid JSON — the draft content is full of double-quotes + HTML + newlines per the formatting rules, which break JSON unless escaped). Fixes: (1) contract now demands a ```json fence + **strictly valid JSON**, explicitly telling the model to escape `\"` and `\n` inside string values; (2) `extractProposal` gained a `repairControlChars` pass that escapes raw newlines/tabs inside string literals before giving up (verified: raw-newline JSON now parses; valid JSON unaffected); (3) on parse failure of a JSON-looking reply, the panel no longer dumps the raw JSON — it shows a clean "couldn't format cleanly — click Put this into Drafto" message and arms the safety-net button (whose retry prompt also re-emphasises valid JSON). NOTE: unescaped *quotes* inside strings can't be auto-repaired reliably; the instruction is the guard, the retry button the fallback. **Ask the user to paste the raw dump if it recurs** to target the exact malformation.
> - **Prompt guidance:** Master Instructions now have Mayur CONFIRM the essentials before drafting a full SLP from a folder — that the folder holds the Impugned Judgment + the HC petition (with annexures); whom the user represents + their HC position (or, if not HC parties, who/why aggrieved); and, for a batch matter, whether the SLP covers all petitions or only some. Mirrored as a user-facing "For the best draft, give me:" tips box in Mayur's empty state.
> - Build + prompt checks clean.
>
> **ROBUST JSON FIX — added `jsonrepair`:** The hand-rolled repair only fixed newlines, so unescaped double-quotes in the quote-heavy drafts (citations, `"ITBP"`, `"Commutation Rules"`) still failed → user hit the "couldn't format cleanly" message. Added the **`jsonrepair`** library (pure JS, bundles fine) into `tryParse` (and the salvage object-parser): strict `JSON.parse` first, then `jsonrepair`. Verified it parses the exact real-world failure (unescaped quotes around `"Commutation Rules"` + `<b>"ITBP"</b>` + a raw newline) and recovers the value intact. Removed the now-redundant `repairControlChars`. The original Act turn should now parse directly (no failure message); strict-JSON instruction + retry button remain as defense-in-depth.
>
> **REMOVED Talk/conversation mode (user directive):** Mayur is now a pure field-filler — every request produces a one-line note + JSON proposal → review card; no chat, no discussion, no drafts-as-prose. System prompt rewritten: "Your one and only job" = propose field edits; "You are NOT a chatbot… no discussion mode"; a question that's really a fill request ("what should the grounds be?") is treated as an instruction to draft those fields. Removed the dual-mode section, the "just wants to chat → no JSON" hard rule, and the "ask the essentials" instruction (now: determine from documents, assume + note, ask only as a last resort if truly blocked). Empty-state reworded to action-oriented. Safety-net button + jsonrepair retained. Build + prompt checks clean.
>
> - **Test path:** save the project first (so files land next to it) → pick a folder with a consolidated PDF → ask the assistant to "split and attach the documents" (or draft the full SLP) → review card with thumbnails → Split & attach → verify annexures land on the right LoD rows chronologically and the files are in the `<project>-annexures` folder. **Recommend Sonnet/Opus for accurate page-boundary detection.**

⬜ **[Outstanding]** AI Plugin: Currently, Drafto has no AI elements in it. I want there to be an AI plugin which users can enable or disable from the Customization tab. This should appear as a chat box in Drafto at the bottom right. Drafto will not provide any AI assistance, but the plugin should let the user activate their existing AI subscriptions (with Claude Code, say) within Drafto. The AI Plugin should have all the necessary instructions fed into it to ensure that, regardless of the LLM connected by the user, the plugin knows exactly where what is in Drafto. So, e.g., someone should be able to dump their raw, cluttered PDF files into the plugin (or simply point the plugin to a folder on the local disk), from which the plugin will auto-populate all the fields in Drafto.
>
> **Decision (2026-06-03):** Connection model = **Claude Code CLI (bring-your-own-subscription)**, shipped as **Beta**. Drafto shells out to the user's locally installed `claude` CLI — Drafto never stores an API key; the user's own Claude login does auth + billing. An optional API-key fallback may be added later for "any LLM", but is **not** in the first cut.
>
> **Prerequisites surfaced to the user (Customize tab + onboarding):**
> 1. **Node.js (LTS)** installed.
> 2. **Claude Code CLI** installed (`npm i -g @anthropic-ai/claude-code`) with the `claude` command on PATH.
> 3. A **logged-in / authenticated** Claude Code session (active Claude Pro/Max plan or API credits) so `claude` runs non-interactively without prompting for login.
> 4. The `claude` binary **discoverable by Drafto** — on PATH, or the user points Drafto at the binary path in Settings.
> 5. **Local read access** to the folder of source PDFs to be ingested.
> 6. **Internet connection** (the CLI calls Anthropic).
> 7. OS: macOS / Windows (Drafto's existing targets).
>
> Drafto must **detect** items 1–4 and show a clear "prerequisites not met" state with copy-paste install/login instructions rather than failing silently.
>
> **Progress — Layer 1 done (Customize tab + prerequisite detection):**
> - New **Settings → Customize** section (nav row added between Paperbook and Save Locations).
> - "AI Plugin (Beta)" block: enable/disable toggle, optional `claude` binary-path override, install instructions (Node, `npm i -g @anthropic-ai/claude-code`, login, internet), and a Pro-Tip noting the assistant won't overwrite work without confirmation.
> - Live **prerequisite detection**: new IPC `ai-check-prerequisites` in `electron/main.js` probes `node` and resolves `claude` (custom path → `which`/`where` → PATH), with a **PATH-augmented env** so Homebrew/nvm/volta/npm-global installs are visible to the GUI app on macOS. Exposed via `preload.js` (`aiCheckPrerequisites`) and typed in `electron.d.ts` (`AiPrerequisites`). Auto-runs when the tab opens; `PrereqRow` shows green/amber/red per item. Settings persist (`aiPluginEnabled`, `aiClaudeBinaryPath`).
> - Vite build passes; no new type errors.
> **Progress — Layer 2 done (bottom-right chat panel):**
> - New `src/components/custom/ai-chat-panel.tsx`, mounted in `drafto-client.tsx` beside `FindReplaceBar` (inside `FormProvider`, so later layers can read/write form data). Renders only when `aiPluginEnabled`; reacts live to the `drafto-settings-changed` event.
> - Collapsed pill launcher (bottom-right) ↔ expanded 360×480 panel: header w/ Beta badge + minimize/close, transcript with empty-state hint, "point me at a folder" picker (uses `selectDirectory`) shown as a clearable chip, auto-growing composer (Enter to send, Shift+Enter newline), and a persistent "review suggestions before saving" footer.
> - Opening the panel re-runs the prerequisite check; shows an amber "not detected → Settings → Customize" banner or a green "connected" banner accordingly, and disables the composer until ready.
> - **Honest stub:** send currently echoes the user message + a clearly-labelled system note that the live connection is coming — no fake AI answers. Clean boundary for the Layer 4 bridge to replace.
> - Also softened the Customize-tab install instructions: now recommends the native installer (`curl … | bash`) first and warns that the npm route can fail with EACCES (the permission error encountered on the test Mac), with a "skip if you already use Claude Code" note.
> - Vite build passes.
>
> **Progress — Layer 3 done (Drafto knowledge + tool contract + validation):** new `src/lib/ai/`:
> - `field-catalog.ts` — a curated, safety-bounded allow-list of fillable fields (Basic / SLP / Listing Proforma), each with path, kind, enum values and (for tables) row shape. Doubles as the prompt's field map and the validation allow-list, so file/ID/annexure state is unreachable.
> - `drafto-knowledge.ts` — `buildSystemPrompt()` renders the catalog + a strict JSON output contract + hard safety rules (no invented facts, can't attach files, omit-if-unsure) into model-agnostic instructions.
> - `form-patch.ts` — `validateProposal()` (allow-list + per-field type/enum/ISO-date checks, partial rows allowed), `buildPreview()` (before/after for the review UI), `extractProposal()`/`stripJsonBlock()` (pull JSON out of the reply). Verified with a runtime test: good ops accepted; bad enum, unknown path and bad date all rejected.
>
> **Progress — Layer 4 done (CLI bridge + suggest-before-write):**
> - New IPC `ai-run` in `electron/main.js`: spawns the user's `claude` headless (`-p --output-format json --permission-mode plan`), injects the Drafto knowledge via `--append-system-prompt`, grants the chosen folder via `--add-dir`, pipes the message over stdin. **Plan mode = read-only**, so pointing it at a PDF folder can never modify files. Parses the JSON envelope (`is_error`/`result`), detects "not logged in", 180s timeout. Exposed as `aiRun` in preload + typed in `electron.d.ts`.
> - `apply-ops.ts` writes approved ops via react-hook-form `setValue`, passing list rows through their Zod item schema (fresh IDs, defaults, ISO-date→Date). (`aamTableItemSchema` exported from `schema.ts` for this.)
> - Chat panel now: sends → spawns CLI → shows the assistant's prose → renders a **Suggested changes** review card (per-field before/after, list-row preview, skipped-invalid count) with **Apply / Dismiss**. Nothing touches the form until Apply; Apply fills fields but never saves — the user still saves the project. Login/error states surfaced inline.
> - Vite build passes; `node --check` clean on main.js/preload.js.
>
> **Login detection + one-click sign-in added:** Diagnosed on the test Mac that the user runs Claude via the *Desktop* app, whose login isn't shared with the standalone `claude` CLI Drafto spawns (no `~/.claude/.credentials.json`, no Keychain item) → "not logged in". The standalone CLI needs its own sign-in.
> - Found clean subcommands: `claude auth status --json` (→ `{loggedIn,...}`, instant + **free**, no inference call) and `claude auth login --claudeai` (browser OAuth).
> - `ai-check-prerequisites` now always runs `aiAuthStatus` when the binary is found (free), returning `loggedIn`/`authMethod`/`needsLogin`; "Ready" requires signed-in. Removed the earlier paid-ping approach.
> - New `ai-login` IPC opens a **Terminal** running `claude auth login` (macOS via `osascript`; Windows `cmd /k`; Linux common emulators) so the user signs in without typing commands.
> - Customize tab: a "Signed in" status row, a prominent **"Sign in to Claude Code"** button shown when installed-but-not-signed-in, and step-by-step manual instructions (exact `claude auth login` command, how to open Terminal, note that Desktop-app login doesn't count). Panel's not-logged-in message updated to point at the button.
> - Build + `node --check` clean.
>
> **✅ VERIFIED (2026-06-03):** End-to-end text-fill confirmed in the real app — user told the assistant to set the SLP type to Criminal and it applied correctly via the review card. Sign-in via the "Sign in to Claude Code" button worked. Layers 1–4 done & verified.
>
> **Streaming + cancel added (fixes heavy-task timeout):** A "draft a full SLP from a judgment + writ petition" prompt timed out on the old flat 180s timeout with no feedback. Reworked `ai-run` to `--output-format stream-json --verbose`: parses line-delimited events (`system init` / `assistant` text+tool_use / `result`), forwards human-friendly progress to the renderer via `ai-stream` (e.g. "Reading judgment.pdf…", "Drafting…"), and replaced the flat timeout with a **4-min idle timeout** (reset on every event) so long-but-active runs aren't killed. Added `ai-cancel` IPC + a **Stop** button, and a "Stopped." result path. Preload exposes `aiCancel` + `onAiStream` (disposer); panel shows live status in the spinner row. CLI event shapes verified directly; build + `node --check` clean. (Token-by-token partial streaming via `--include-partial-messages` left as a possible later refinement.)
>
> **PDF text extraction + token gating (fixes the "install poppler?" problem):** A folder prompt stalled with Claude asking the user to `brew install poppler` to read a PDF. Reasoned through with the user: Claude *can* read un-OCR'd images, but image-reading is token-expensive, so text extraction is the cheap path and image-reading should be gated. Decision: Drafto extracts text itself; ask-every-time before reading scanned pages as images.
> - Added `pdfjs-dist` (pure-JS production dep; `asarUnpack`-ed so its ESM loads in packaged builds). New `electron/ipc/pdf-extract.js#scanFolder`: extracts per-page text into a temp context dir of `.txt` files (page markers), classifies pages with <15 chars as scanned, estimates tokens (text ≈ chars/4; image ≈ 1500/page). Verified in Node **and** with a real `claude` round-trip (it read the .txt and returned a valid petitioners/respondents/caseType proposal — no poppler, no PDF reading by Claude).
> - IPC `ai-scan-folder`; `ai-run` extended to accept `addDirs[]` (context dir + optionally the original folder for scanned-page images). System prompt gained `sourceNote` telling the model where the extracted text is and which scanned pages to read as images.
> - Panel: picking a folder scans it (chip shows "N PDFs · ~Xk tokens of text · N scanned"). On send, if there are scanned pages, an **ask-every-time** confirmation card appears (Read scanned pages ≈Y tokens / Text only / Cancel) before any image reading — the user's chosen default. Cost framed as *usage against the Claude subscription*, not dollars. Composer gated during scan/confirm. Build + `node --check` clean.
> - **Follow-on if needed:** scanned-page reading delegates to Claude's vision on the original PDF (no Drafto-side image rendering / Tesseract yet); fine for moderate docs, revisit for very large scanned files.
>
> **BUGFIX — empty context dir / pdf.js Node-version mismatch:** First real run produced an *empty* context dir (Claude reported "the source folder is empty"). Root cause: **pdfjs-dist v6 uses `Promise.withResolvers` (Node 22+), but Electron 28 bundles Node 18**, so extraction threw inside Electron (it worked in the dev Node 24 shell — that's why tests passed). Fix: **downgraded to `pdfjs-dist@3.11.174`** and switched `pdf-extract.js` from ESM dynamic `import()` to `require()` of the CommonJS `legacy/build/pdf.js` (Node-18-safe; also dodges ESM-in-Electron-main issues). Set `GlobalWorkerOptions.workerSrc` to the worker module (fake-worker fallback if it fails). Also hardened: if **no** PDF extracts, `scanFolder` now returns `ok:false` with a clear error and deletes the temp dir, instead of silently handing Claude an empty folder. Re-verified scanFolder via `require()` + build + `node --check`. Lesson: test against the Electron-bundled Node version, not just the dev shell.
>
> **THREE FIXES from a real party-extraction test** (parties came back correct but mislabelled; "modify" requests failed):
> 1. **Conversation had no memory (the big one).** Each turn spawned a fresh `claude -p` with no continuity, so "modify accordingly" hit a blank session — the model even asked for the path to Drafto's *source code* and just re-ran the extraction. Fix: capture the CLI `session_id` and pass `--resume <id>` on subsequent turns (skip `--append-system-prompt` on resume; re-grant `--add-dir` each turn). `ai-run` returns `sessionId`; panel carries it turn-to-turn and resets it on folder change. **Verified headless** that a resumed session remembers prior context (codeword test).
> 2. **Model drifted into coding-agent behaviour.** Added hard rules to the system prompt: it is a form-filling assistant, must NEVER look for/read/modify Drafto's source code or ask for the project path, and "fix/modify/correct" means re-issue a corrected JSON proposal (re-reading the source doc), not touch code.
> 3. **`positionInEarlierCourt` mislabelled.** Field guidance said "e.g. Plaintiff/Appellant", so the model wrote bare "Petitioner"/"Respondent". Rewrote it to require the numbered designation ("Petitioner No. 1", "Respondent No. 3") taken from the source memo of parties.
> Build + `node --check` clean. Note: improvements apply to **new** sessions — after restart, re-pick the folder to start a fresh conversation with the updated rules.
>
> **FIX — plan mode stalled with "once approved, I'll emit the JSON":** `--permission-mode plan` has a second behaviour beyond read-only — it makes the model present a plan and wait for an approval step that doesn't exist in headless mode, so it never emitted the proposal. Replaced plan mode with **`--permission-mode default` + `--tools Read,Glob,Grep` + `--allowedTools Read,Glob,Grep`**: the toolset is restricted to read-only tools (no Write/Edit/Bash/ExitPlanMode at all — a *stronger* read-only guarantee than plan mode), reads run without prompts, and there's no plan-gating. Verified at the CLI: it now reads the .txt and emits the JSON proposal directly in one turn, with correct numbered positions ("Petitioner No. 1/2"). Build + `node --check` clean.
>
> **FIX — auto-memory leak / "spiral" reading MEMORY.md & feedback_*.md:** The spawned CLI was auto-loading the user's Claude Code project memory (dev notes about DraftoSLP) and wandering off to read linked files. Diagnosis: every isolation lever (`--bare`, `CLAUDE_CONFIG_DIR`, `CLAUDE_CODE_SIMPLE=1`) breaks OAuth/subscription auth (creds are in the macOS keychain but still tied to the default config dir). The one thing that cleanly disables memory is a **clean working directory** — and `ai-run` was falling back to `os.homedir()` when no folder was selected. Fix: spawn `cwd` is now always a clean dir — the extracted-text context dir, or an empty `os.tmpdir()/drafto-ai-cwd` scratch dir — **never home/project**. Verified: clean cwd → "NONE" memory loaded. (Also clarified to the user: `--resume` keeps the *whole* chat in context, so there's no 3-message recall limit — the spiral was the memory issue, not a memory cap.)
>
> **Model selector added.** `ai-run` accepts `model` (haiku/sonnet/opus; "default"/unset → CLI default = Sonnet) → `--model` flag (verified `--model haiku` switches to claude-haiku-4-5). New `aiModel` setting (persisted) with a dropdown in Settings → AI Plugin (Haiku=fast/light, Sonnet=balanced/recommended, Opus=strongest/heaviest, plus "Default"), and a compact model switcher in the chat panel header that writes the same setting. Panel passes the chosen model each turn. Build + `node --check` clean.
>
> **Master Instructions (editable domain rules) + completeness + per-task cost warning:**
> - New **`src/lib/ai/master-instructions.ts`** — a clearly-marked, user-editable `MASTER_INSTRUCTIONS` string injected verbatim near the top of the system prompt. Encodes the user's 7 rules: (1) Drafto only drafts SLPs challenging an Impugned Order (usually a High Court's); (2) HC-record docs get annexed, extras flagged "AD"; (3) "draft" = FILL FIELDS, not write in chat (default instinct = fill fields unless explicitly asked for chat text); (4) substantive fields drafted to challenge the Impugned Order; (5) criticise the Impugned Order, never the court ("the Impugned Order is erroneous because…", not "the High Court erred"); (6) "the Hon'ble High Court of ___" / "this Hon'ble Court"; (7) ask the user when a detail/document is missing. Verified all present in the built prompt.
> - **Completeness rule** added (addresses Haiku delivering a partial draft + "you should review the writ petition to capture all grounds"): always complete the whole job, never hand work back, don't cut corners to save tokens.
> - **Per-task cost warning** generalised: the confirmation card now appears before any substantial folder task (scanned pages **or** ≥20k text tokens), showing the upper-bound text-token estimate (+ scanned-image cost) and Proceed/Text-only/Cancel — so the user consciously approves a thorough (costlier) run instead of the model silently truncating.
> - **Caveat to flag to user:** Haiku is the weakest model and most prone to truncating large sources even with the completeness rule; Sonnet (default) / Opus recommended for full drafting. Build + bundle-presence + runtime prompt checks all pass.
>
> **Drafting-quality + UX batch (after a successful Haiku draft):**
> - Confirmed substantive fields (Synopsis/Grounds/QoL/LoD particulars/Interim Relief) are **tiptap rich-text editors storing HTML** that re-render on external `setValue` — so bold/italic via HTML markup in proposed values works.
> - **Master Instructions additions** (all verified in built prompt): SC nomenclature fixed to cover possessives/own judgments ("this Hon'ble Court's judgment", never "the (Hon'ble) Supreme Court's"); party names in smart **Title Case** ("UNION OF INDIA"→"Union of India", lowercase of/and/the/v.); **never fill AoR name/code** unless explicitly named; **formatting** — use HTML `<b>/<i>` only in rich fields (plain text for names etc.), **case names & citations always `<b><i>…</i></b>`** (full or supra), **double-quoted text italic** with the **exception that bracketed defined abbreviations are bold** (e.g. `<b>"ITBP"</b>`, `<b>"Commutation Rules"</b>`).
> - **Settings → User Defaults** (new tab): AoR Name + AoR Code; persisted (`defaultAorName`/`defaultAorCode`). New `src/lib/project-defaults.ts#newBlankProject()` applies them to the launch project (`drafto-client` defaultValues) and to **New Project** (`header.tsx handleNew`).
> - Renamed Settings nav "Document Formatting" → **"Formatting"**.
> - Clarified to user: the "~182k" is a per-folder display **estimate** (chars/4), NOT a hardcoded cap; the assistant reads as much as needed.
> - Build + runtime prompt checks pass.
>
> **Bugfix batch from a Haiku docx test (screenshots):**
> - **DOCX line-break bug (root cause + fix):** the assistant's proposed rich-text values are stored as RAW HTML (TipTap's `setContent(value,false)` doesn't write back, so the form field keeps the model's un-`<p>`-wrapped HTML). `html-to-docx.ts treeToDocx` turned each loose top-level string/inline node into its OWN paragraph (and dropped the bold/italic), so every formatting boundary became a new line. Fix: buffer consecutive inline/string nodes and flush them as ONE paragraph via `processInline` (same path as the `<p>` case). Verified: loose-inline HTML now yields a docx paragraph **byte-identical** to the well-formed `<p>` version; well-formed/toolbar content unchanged (1 `<p>`→1 para, 2→2). `processInline` already handled `<b>/<i>`. So the AI can keep emitting HTML and it converts correctly — answered the user's "use the toolbar" ask: the toolbar's output is the same HTML; the breakage was a converter gap, now closed.
> - **Master Instructions tightened:** emphasis must be applied TIGHTLY (only the citation/quote/defined-abbrev word(s), never a whole sentence — fixes Haiku bolding the entire first line); AoR fields must not even be raised/asked about (Haiku asked "same lawyer as in HC?"); Title-Case rule now explicit that "of/and/the/v./for/in/on" stay lowercase ("Union of India", NOT "Union Of India").
> - Removed the personal placeholder text (name/code) from the User Defaults inputs → generic placeholders.
> - Caveat stands: these were all on **Haiku**; Sonnet/Opus follow such style rules far more reliably.
> - Build + runtime prompt checks pass.
>
> **Status: Task 14 text-fill feature-complete (Beta), VERIFIED. (Earlier note retained:) pending user verification with a live logged-in `claude` (the sandbox CLI here reports "Not logged in", so the end-to-end model round-trip must be confirmed in the real app).**
>
> **Planned — Layer 5 (PDF split-plan), agreed but DEFERRED until after the user verifies Layers 1–4:**
> Real use needs the assistant to split a consolidated raw PDF into individual annexures (attached to List-of-Dates / IA-ground rows) and to separate per-petitioner affidavits & vakalatnamas. **Decision: split-plan, NOT giving Claude write access.** Claude stays read-only (`plan` mode) and proposes a *document map* (page ranges + classification + suggested title/date + target slot); the user reviews/corrects the page map; Drafto then cuts the files deterministically with `pdf-lib` (`copyPages`) into a dedicated output folder (source never modified) and wires the new `filePath`s into the schema. Bounds-check ranges vs. real page count, flag overlaps.
> - Annexure attachment slots are known: `annexureSchema.filePath` inside `listOfDates[].annexures` and `iaGroundItem.annexures`.
> - **To confirm at build time:** how affidavits & vakalatnamas store files — they go through their own dialogs (`affidavit-dialog.tsx`, `vakalatnama-dialog.tsx`); need to check whether those attach a file path into the schema or are Drafto-generated, which decides whether per-petitioner affidavit/vakalatnama *attachment* is in scope.