---
name: project_entitlement_enforcement
description: "Drafto entitlement layer BUILT (Phase 0, uncommitted) — reads users/{uid}, gates edit+export; pre-ship it needs accessOverride data step + URL confirm"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0bb63d65-57a2-41fd-964d-df3927f070c9
  modified: 2026-08-03T19:26:23.848Z
---

**ORIGINAL PROBLEM (pre-2026-07-22):** the app enforced NO subscription/plan/expiry — Firebase Auth sign-in only; nothing read `users/{uid}`. Only lever was disabling the Auth account (`authService.validateToken` every 5 min → force-logout on `auth/user-disabled`/`user-not-found`), and 0 of 22 accounts were disabled → every signup kept permanent full access. The PDF dialog's `validateProjectForPdf` only checks document COMPLETENESS, not billing (explains "couldn't generate PDF but could use other features" = missing annexures, not expiry).

**PHASE 0 BUILT 2026-07-22 (working tree, NOT yet committed/shipped):** client-side entitlement layer, all in `~/DraftoSLP`, `npx vite build` green. Policy (user-chosen): lapsed→READ-ONLY (view but no edit/export), halted/paused→7-day grace from `currentPeriodEnd` then read-only, 7-day offline grace.
- `src/lib/entitlement/entitlement.ts` — pure `resolveEntitlement(billing, nowSec)`; supports `suiteAccessUntil` (3-month migration full-suite grant) + `accessOverride`/`overrideTier`/`overrideUntil` (manual per-user override).
- `src/lib/firebase/entitlement-service.ts` — fetch `users/{uid}`, normalise Firestore Timestamps→epoch sec, localStorage cache `drafto.entitlement.<uid>` + offline grace.
- `src/providers/entitlement-provider.tsx` — `EntitlementProvider` (wraps DraftoClient), `useEntitlement`/`useCanEdit`/`useCanExport`, 15-min + on-focus refresh, `MANAGE_SUBSCRIPTION_URL='https://drafto.quindoph.com/account'` (PLACEHOLDER — confirm real route).
- Gates: read-only lock via `src/components/custom/read-only-lock.tsx` (`ReadOnlyLock` wraps Workspace in drafto-client — ONE wrapper, so SC+HC behave identically). Capture-phase guard blocks `beforeinput`/`paste`/`cut`/`drop` (all text incl. tiptap) + `click`/`keydown` on mutation controls (buttons/checkboxes/selects) EXCEPT navigation. Nothing is disabled or blurred; reading/selecting/copying/scrolling and tab/section nav still work. Nav helpers (ViewToggle/NavRow in slp-tab, basic-tab, listing-proforma-tab, wp-workspace) marked `data-ro-nav`; Radix tabs pass via `role="tab"`. Tiptap `BadhiyaBox` also honors `useCanEdit()` (contentEditable can't be locked by the guard/fieldset alone). Banner `entitlement-banner.tsx` is `text-xs`. NOTE: earlier `<fieldset disabled>` approach was REMOVED — it disabled tab/nav buttons and triggered tables' `group-disabled:blur-sm` (HC "everything blurred") and missed the tiptap Synopsis. Known minor gap: dnd-kit row REORDER (pointerdown drag) not blocked in read-only. Export blocked in `downloadDocx` (header), paperbook `onSubmit` (pdf-generation-dialog), wp-pdf dialog; AI drafting blocked via `blockIfReadOnly` in ai-chat-panel `submitText`+`applySuggestions`; Find&Replace `handleReplaceCurrent`/`handleReplaceAll`.

**MULTI-DEVICE / SEAT ENFORCEMENT BUILT 2026-07-22 (same working tree, build green):** replaced the old single-device flat `sessions/{uid}` (one `deviceId`, new login force-logs-out the old) with a per-device MAP inside `sessions/{uid}` — `devices.{deviceId}` = {deviceInfo, loginTime, lastHeartbeat}. Deliberately reuses the `sessions/{uid}` doc path so the DEPLOYED Firestore rules (`match /sessions/{userId}` owner r/w) already permit it → **NO firestore rules deploy needed** (a subcollection WOULD have needed one; the catch-all denies everything else). Reads `users/{uid}.deviceLimit` (webhook-written: solo 1/chamber 3/enterprise 5). Policy: at limit a NEW device is BLOCKED (not evicted); seats freed only by explicit/remote sign-out via in-app "Manage devices" (no inactivity auto-reclaim). Real-time via `onSnapshot` (remote sign-out → instant force-logout; blocked device auto-admits when a seat frees). Device check FAILS OPEN when offline. Files: `src/lib/firebase/device-service.ts` (transactional register), `src/hooks/use-auth.ts` (drives register+heartbeat+listener on auth-state, so also runs on reload), `src/components/auth/manage-devices.tsx` + `device-limit-screen.tsx`, `ProtectedRoute` gates on `deviceStatus`, Settings→Support shows ManageDevices. auth-service `signIn`/google/`signOut` stripped of old flat-session logic (kept token `startPeriodicValidation`); dead private methods (startHeartbeat/updateHeartbeat/listenToSessionChanges) + `getIpAddress` left unused — optional cleanup.

**DEV SIMULATOR BUILT 2026-07-22:** `src/lib/dev/sim-entitlement.ts` (gated `SIM_ENABLED = import.meta.env.DEV || VITE_ENABLE_DEVSIM==="true"`, mirroring [[project_writ_petition_dhc]]'s wp-enabled pattern) + floating `src/components/dev/dev-sim-panel.tsx` mounted at ROOT in `main.tsx` (so it's reachable even on the device-limit blocked screen, which replaces DraftoClient). Scenarios: active-solo/chamber, trial, grace (halted in-window), past-grace, lapsed/expired, cancelled-period-remaining, no-sub, test-override, suite-migration (lite+suiteAccessUntil), and device seats 1of3/2of3/full/solo-full. `fetchEntitlement` short-circuits to `resolveEntitlement(sim.billing)` when a scenario is active; use-auth `applyDeviceSim` fabricates the device list/limit and skips real registration; `simRemoveOtherDevice` makes the blocked-screen "Remove" demo the auto-admit. Broadcasts via `SIM_EVENT`; providers re-resolve instantly. In PROD builds SIM_ENABLED is statically false → dead-code-eliminated, `getSim` returns OFF first line, cannot be enabled via localStorage.

**PRE-SHIP BLOCKERS:**
1. DONE 2026-07-22: `shrutanjaya@gmail.com` set `accessOverride:'full'`+`overrideTier:'full'` (kept as working test acct). ALL OTHER no-`subscriptionStatus` accounts (owner's other logins + legacy one-time users) INTENTIONALLY left to resolve read-only per user's decision ("block the other test and legacy accounts"). To allowlist more later: set `accessOverride:'full'` on that `users/{uid}` doc (no redeploy; app reads live).
2. **CLOSED — verified 2026-08-04 against `origin/main` of quindoph-website.** `MANAGE_SUBSCRIPTION_URL='https://drafto.quindoph.com/account'` is correct and the page now does real self-serve: `src/app/api/subscription/{renew,cancel,change-plan}/route.ts` + `src/components/account-client.tsx` with status derived from Razorpay (falling back to the Firestore record), not the old hardcoded "Active". Renew creates a fresh no-trial subscription and refuses when a live one exists (`active|authenticated|pending|halted|paused` → 409), which was the double-bill risk. **Do not repeat the old "renew goes nowhere" warning — it was true only until ~late July 2026.**
3. Stale record `arayan_khare` Firestore `active` vs Razorpay `paused` — app trusts Firestore, keeps access (safe to leave).

Entitlement verified vs live data 2026-07-22: paying/trial→full; cancelled+expired→read-only; `suditibansal93` cancelled but `currentPeriodEnd` future→full; `crosshair20` halted no period→read-only.

**Why:** tiered pricing for the multi-doctype expansion ([[project_writ_petition_dhc]]) needs enforceable entitlements + seats. **How to apply:** finish blocker #2 → commit → release. All Phase-0 work is UNCOMMITTED on working tree. See [[reference_drafto_subscription_stack]].
